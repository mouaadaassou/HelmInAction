apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}-secret
data:
  mongodb-username: {{ .Values.sercret.mongodb_username }}
  mongodb-password: {{ .Values.sercret.mongodb_password }}
